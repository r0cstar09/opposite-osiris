---
const currentYear = new Date().getFullYear();
const lastSync = new Date().toISOString().slice(0, 10);
---

<footer>
	<div class="group">
		<p>Designed & developed in Toronto.</p>
		<p>&copy; {currentYear} Tony Muzo</p>
	</div>
	<p class="sysline" aria-hidden="true">
		<span class="ok">●</span> sys.status: nominal · last_sync: {lastSync}
	</p>
	<p class="socials">
		<a href="https://github.com/r0cstar09">GitHub</a>
		<a href="https://www.linkedin.com/in/anthony-muzonzini-20702/">LinkedIn</a>
		<a href="mailto:tony@tonymuzo.dev">Email</a>
	</p>
</footer>
<style>
	footer {
		display: flex;
		flex-direction: column;
		gap: 1.25rem;
		margin-top: auto;
		padding: 2.5rem 2rem;
		text-align: center;
		color: var(--text-muted);
		font-size: var(--text-sm);
		border-top: 1px solid var(--border);
	}

	footer a {
		color: var(--text-muted);
		text-decoration: none;
		transition: color var(--theme-transition);
	}

	footer a:hover,
	footer a:focus {
		color: var(--accent);
	}

	.group {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.sysline {
		font-family: var(--font-mono);
		font-size: 0.7rem;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		color: var(--text-muted);
	}

	.sysline .ok {
		color: var(--success);
	}

	.socials {
		display: flex;
		justify-content: center;
		gap: 1.5rem;
		flex-wrap: wrap;
	}

	@media (min-width: 50em) {
		footer {
			flex-direction: row;
			align-items: center;
			justify-content: space-between;
			padding: 2.5rem 5rem;
		}

		.group {
			flex-direction: row;
			gap: 1.5rem;
			flex-wrap: wrap;
		}

		.socials {
			justify-content: flex-end;
		}
	}
</style>
