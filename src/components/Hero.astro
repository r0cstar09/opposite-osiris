---
interface Props {
	title: string;
	tagline?: string;
	eyebrow?: string;
	align?: 'start' | 'center';
}

const { align = 'center', eyebrow, tagline, title } = Astro.props;
---

<div class:list={['hero stack gap-4', align]}>
	<div class="stack gap-2">
		{eyebrow && <p class:list={[align === 'start' ? 'hud-label' : 'mono-label', 'eyebrow']}>{eyebrow}</p>}
		<h1 class="title">{title}</h1>
		{tagline && <p class="tagline">{tagline}</p>}
	</div>
	<slot />
</div>

<style>
	.hero {
		font-size: var(--text-lg);
		text-align: center;
	}

	.title,
	.tagline {
		max-width: 40ch;
		margin-inline: auto;
	}

	.eyebrow {
		margin-bottom: 0.5rem;
		justify-content: center;
	}

	.title {
		font-size: var(--text-3xl);
		color: var(--text-strong);
	}

	.tagline {
		color: var(--text-muted);
		font-size: var(--text-md);
		line-height: 1.65;
		max-width: 54ch;
		text-wrap: pretty;
	}

	@media (min-width: 50em) {
		.start {
			text-align: start;
		}

		.start .title,
		.start .tagline {
			margin-inline: unset;
		}

		.start .eyebrow {
			justify-content: flex-start;
		}

		.title {
			font-size: var(--text-4xl);
		}

		.tagline {
			font-size: var(--text-lg);
		}
	}
</style>
