<div class="pill"><slot /></div>

<style>
	.pill {
		display: flex;
		align-items: center;
		padding: 0.4rem 1rem;
		gap: 0.5rem;
		color: var(--accent-regular);
		border: 1px solid color-mix(in srgb, var(--accent-regular) 55%, transparent);
		background: color-mix(in srgb, var(--accent-regular) 10%, transparent);
		border-radius: 999rem;
		font-family: var(--font-mono);
		font-size: var(--text-sm);
		font-weight: 500;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		line-height: 1.35;
		white-space: nowrap;
		box-shadow: inset 0 0 12px color-mix(in srgb, var(--accent-regular) 12%, transparent);
		transition:
			box-shadow var(--theme-transition),
			border-color var(--theme-transition),
			color var(--theme-transition);
	}

	.pill:hover {
		color: var(--gray-0);
		border-color: var(--accent-regular);
		box-shadow:
			inset 0 0 16px color-mix(in srgb, var(--accent-regular) 20%, transparent),
			0 0 16px color-mix(in srgb, var(--accent-regular) 45%, transparent);
	}
</style>
